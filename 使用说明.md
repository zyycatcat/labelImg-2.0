---
title: "工程领域像素图纸半自动化标注工具"
date: 2023-01-10
author: 
  name: Liu Jingping
  link: https://github.com/xiaoliou008
categories: [Windows, IDEA]
tags: [labelImg, pascal_voc]
---

#### 工程领域像素图纸半自动化标注工具

本项目是基于labelImg开源代码修改得到的，主要增加了模板匹配算法，来简化对于高像素匹配度的目标的标注过程，降低标注成本，提升标注精度和数据质量。



##### 使用方法

直接运行python源码：

```bash
python labelImg.py
```

有关“智能框吸附”功能算法：
  - 使用 mobileSAM 模型

  - 使用传统 cv 方法：针对框内图像，使用3种阈值方法同时处理，然后取并集（保留所有识别出的像素），噪声处理之后计算最小包围矩形

    ```
    # 策略1：自适应阈值 - 捕获局部对比度变化
    adaptive = cv2.adaptiveThreshold(gray, ...)
    
    # 策略2：Otsu阈值 - 全局最优二值化
    _, otsu = cv2.threshold(gray, 0, 255, THRESH_OTSU)
    
    # 策略3：固定阈值 - 捕获深色像素（黑线）
    _, fixed = cv2.threshold(gray, 200, 255, ...)
    
    # 取并集：任何方法识别出的像素都保留
    combined = bitwise_or(adaptive, otsu, fixed)
    ```
